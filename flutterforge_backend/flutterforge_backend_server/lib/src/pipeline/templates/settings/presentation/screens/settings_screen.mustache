import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';
{{#isBloc}}
import 'package:flutter_bloc/flutter_bloc.dart';
import '../bloc/settings_cubit.dart';
{{#hasAuth}}
import '../../../../core/di/injection.dart';
import '../../../auth/presentation/bloc/auth_bloc.dart';
import '../../../auth/presentation/bloc/auth_event.dart';
{{/hasAuth}}
{{/isBloc}}
{{#isRiverpod}}
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../riverpod/settings_notifier.dart';
{{#hasAuth}}
import '../../../auth/presentation/riverpod/auth_notifier.dart';
{{/hasAuth}}
{{/isRiverpod}}
{{#isProvider}}
import 'package:provider/provider.dart';
import '../provider/settings_provider.dart';
{{#hasAuth}}
import '../../../auth/presentation/provider/auth_provider.dart';
{{/hasAuth}}
{{/isProvider}}

{{#isRiverpod}}
class SettingsScreen extends ConsumerWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(title: const Text('Settings')),
      body: ListView(
        children: [
          ListTile(
            leading: const Icon(Icons.star),
            title: const Text('Rate this app'),
            onTap: () {
              ref.read(settingsNotifierProvider.notifier).requestReview();
            },
          ),
          const Divider(),
          ListTile(
            leading: const Icon(Icons.info),
            title: const Text('About'),
            onTap: () {
              showAboutDialog(
                context: context,
                applicationName: '{{projectName}}',
                applicationVersion: '1.0.0',
              );
            },
          ),
          {{#hasAuth}}
          const Divider(),
          ListTile(
            leading: const Icon(Icons.logout, color: Colors.red),
            title: const Text('Sign out', style: TextStyle(color: Colors.red)),
            onTap: () {
              ref.read(authNotifierProvider.notifier).signOut();
              context.go('/login');
            },
          ),
          {{/hasAuth}}
        ],
      ),
    );
  }
}
{{/isRiverpod}}

{{#isProvider}}
class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Settings')),
      body: ListView(
        children: [
          ListTile(
            leading: const Icon(Icons.star),
            title: const Text('Rate this app'),
            onTap: () {
              context.read<SettingsProvider>().requestReview();
            },
          ),
          const Divider(),
          ListTile(
            leading: const Icon(Icons.info),
            title: const Text('About'),
            onTap: () {
              showAboutDialog(
                context: context,
                applicationName: '{{projectName}}',
                applicationVersion: '1.0.0',
              );
            },
          ),
          {{#hasAuth}}
          const Divider(),
          ListTile(
            leading: const Icon(Icons.logout, color: Colors.red),
            title: const Text('Sign out', style: TextStyle(color: Colors.red)),
            onTap: () {
              context.read<AuthProvider>().signOut();
              context.go('/login');
            },
          ),
          {{/hasAuth}}
        ],
      ),
    );
  }
}
{{/isProvider}}

{{#isBloc}}
class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return BlocProvider(
      create: (_) => SettingsCubit(),
      child: Scaffold(
        appBar: AppBar(title: const Text('Settings')),
        body: Builder(
          builder: (context) {
            return ListView(
              children: [
                ListTile(
                  leading: const Icon(Icons.star),
                  title: const Text('Rate this app'),
                  onTap: () {
                    context.read<SettingsCubit>().requestReview();
                  },
                ),
                const Divider(),
                ListTile(
                  leading: const Icon(Icons.info),
                  title: const Text('About'),
                  onTap: () {
                    showAboutDialog(
                      context: context,
                      applicationName: '{{projectName}}',
                      applicationVersion: '1.0.0',
                    );
                  },
                ),
                {{#hasAuth}}
                const Divider(),
                ListTile(
                  leading: const Icon(Icons.logout, color: Colors.red),
                  title: const Text('Sign out', style: TextStyle(color: Colors.red)),
                  onTap: () {
                    getIt<AuthBloc>().add(SignOutEvent());
                    context.go('/login');
                  },
                ),
                {{/hasAuth}}
              ],
            );
          }
        ),
      ),
    );
  }
}
{{/isBloc}}
